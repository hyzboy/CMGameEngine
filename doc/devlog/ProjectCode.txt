工程名称与工程代码

	从《古月》17开始，引擎会要求用户在起始时设定“工程名称”与“工程代码”这个字符串值。
绝大部分开发者只是原样照抄示例，很少有问起含义。

	工程名称：故名思义，名称而己。用于各处显示，日志所用。如窗口标题(如无额外指定)。

	工程代码：类似工程名称，但主要用途是给程序使用，如文件名、类名、标识等。他符合一
般程式命名的要求，纯英文数字混合，必须字母开头等。

	比如日志文件，会使用工程代码做为文件名，但在其中的内容上，会写上以工程名称开头的说明。